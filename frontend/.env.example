VITE_API_URL=http://localhost:5000/api

# Google OAuth Client ID, for "Sign in with Google". Leave unset to hide the
# button. Must match the GOOGLE_CLIENT_ID configured on the server.
VITE_GOOGLE_CLIENT_ID=
